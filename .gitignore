/pcaptriage
/pcaptriage.exe
/dist/
*.out

# Reports and previews generated while poking at the fixtures.
/report-*.html
/report-*.json
/preview-*.html

# Wails compiles the app here. The rest of build/ — the icon, manifest and
# info.json — is source and is committed.
/build/bin/

# Generated by the Wails toolchain from the bound Go methods. Nothing in
# frontend/dist imports it: the frontend calls the bindings through the
# window.go and window.runtime globals the runtime installs. Regenerable, so
# not carried in the repository.
/frontend/wailsjs/
